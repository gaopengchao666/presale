cron: 0/10 * * * * ? 
mybatis: 
    mapper-locations: classpath:template/*.xml
    typeAliasesPackage: cn.com.entity

spring: 
    datasource: 
        url: jdbc:mysql://localhost:3306/presale
        username: root
        password: root
        driver-class-name: com.mysql.jdbc.Driver
    mvc: 
       view: 
           prefix: /WEB-INF/jsp/
           suffix: .jsp
